// 특정 위치 문자 반환하기(charAt)
{
  // charAt(index) index 위치의 값을 반환한다.
  const str = "Good afternoon, Good evening, and Good night! ";
  console.log(str.charAt(0));
  console.log(str.charAt(5));
  console.log(str.charAt(100));
  console.log(str.length);
}
